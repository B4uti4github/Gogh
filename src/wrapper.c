// Minimal C wrapper placeholder for Thorvg integration.
// This file exists so CI can compile while the real wrapper is developed.

#include <stdio.h>

// A no-op function to ensure the C source compiles and produces an object.
void gogh_thorvg_wrapper_stub(void) {
    // Intentionally empty
}
